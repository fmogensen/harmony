---
description: Show what would change if you ran harmony apply (read-only diff).
allowed-tools:
  - Bash
---

# /harmony-status

Show drift between your harmony manifest and the actual state of this Mac. Read-only — nothing is changed.

```bash
harmony status
```
